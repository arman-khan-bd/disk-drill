{
  "$schema": "https://schema.tauri.app/schema/2.0.0/capability",
  "identifier": "default",
  "description": "Default capabilities for RescuR app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "process:default"
  ]
}
